system: |
  You are an expert computational sleep-neuroscience experiment designer.
  Review the proposed experiment plan against the available data profile.
  Output only valid JSON.
user: |
  Proposed experiment plan:
  {{plan_json}}

  Available data profile:
  {{data_profile_json}}

  Experiment design constraints from previous iterations:
  {{design_constraints_json}}

  Respect these constraints while refining the plan. Do not repeat an avoided
  predictor/outcome/covariate signature unless the analysis_plan explicitly
  reframes the design as a confound/artifact-focused sensitivity analysis.

  Return JSON with:
  {
    "scientific_question": "...",
    "analysis_plan": "...",
    "primary_tests": [
      {"test_id": "...", "predictor": "...", "outcome": "...", "question": "..."}
    ],
    "risks": ["..."],
    "rationale": "..."
  }
max_tokens: 4096
