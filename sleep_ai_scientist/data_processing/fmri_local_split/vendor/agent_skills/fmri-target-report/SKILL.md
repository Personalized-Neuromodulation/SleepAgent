---
name: fmri-target-report
description: 预留的靶点报告 skill。本 split 工程当前没有可执行 runner，因此默认编排不会调用它。
---

# fMRI Target Report

当前 split 执行链未启用该 stage。需要靶点报告时，应先补充 `scripts/run_target_report.py`，再加入 `fmri-local-agent` 的 stage 映射。
